#include <bits/stdc++.h>
using namespace std;

int n;

bool solve(string s){
	for (int i=1; i<s.size(); i++){
		if (int(s.at(i)) - int(s.at(i-1)) != 1) {
			return false;
		}
	}return true;	
}

int main() {
	int n,a;
	string s;
	cin >> s;
	cout << solve(s) << endl;
}
