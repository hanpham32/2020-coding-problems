#include <iostream>

using namespace std;

int main()
{
	int M, N;
	cin >> M >> N;
	// if(M*N/2 % 2 == 0)
	// {
	// 	cout << M*N/2 << endl;
	// } else
	// {
	// 	cout << (M*N/2)-1 << endl;
	// }
	(M*N%2 == 0) ? cout << M*N/2 << endl : cout << int((M*N/2)) << endl;
}
